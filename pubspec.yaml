name: tick
description: "경량 Todo 앱 — Supabase 기반 계정 동기화"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.9.0
  flutter_riverpod: ^2.6.1
  google_sign_in: ^6.2.1
  sign_in_with_apple: ^6.1.4
  connectivity_plus: ^6.1.4
  crypto: ^3.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
